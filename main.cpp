#include <iostream>
#include "fecha.h"
#include "producto.h"


int main()
{
    /*Fecha f;
    boolean fechaValida;

    cargarFecha(f);
    fechaValida = esValidaFecha(f);
    while (!fechaValida)
    {
        printf ("\nLa fecha no es valida\n");
        cargarFecha(f);
        fechaValida = esValidaFecha(f);
    }
    mostrarFecha(f);*/

    /*Producto p;
    string nombre;
    cargarProducto(p);
    mostrarProducto(p);
    printf ("\n Selectoras: ");
    printf ("\n Nombre: ");
    getNombreProducto(p, nombre);
    print(nombre);
    printf ("\n Numero: %ld", getNumeroProducto(p));
    printf ("\n Precio: %f", getPrecioProducto(p));
    if (getStockProducto(p))
        printf ("\n Está en stock");
    else
        printf ("\n No está en stock");
    mostrarFecha(getFechaProducto(p));*/


}
